# -- Twilio API Creds + Config
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# -- Supabase API Creds + Config
SUPABASE_URL=
SUPABASE_ANON_KEY=

# -- Debugging

## Set to 1 to show the React Query dev tools
NX_CUSTOM_VAR_DEBUG_REACT_QUERY=1